# Copier ce fichier en `.env` et remplir avec les valeurs réelles.
# Le fichier `.env` est exclu par le .gitignore — ne jamais le committer.

GLPI_URL=https://your-glpi.example.com/apirest.php/
GLPI_APP_TOKEN=your-app-token
GLPI_USER_TOKEN=your-user-token
