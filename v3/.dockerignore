# Fichiers présents dans le build context mais non servis par nginx.
# L'entrypoint et le template restent dans le context car le Dockerfile.prod
# les COPY hors de /usr/share/nginx/html/.
Dockerfile.prod
.dockerignore
README.md
docker-compose.prod.yml
.git
.gitignore
